document.getElementById('download-cv-btn').addEventListener('click', function () {
    // Ajoutez ici le code pour générer le PDF et le télécharger
    // Utilisez une bibliothèque comme jsPDF ou html2pdf pour convertir la page HTML en PDF
});
